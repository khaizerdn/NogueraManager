import { View, Text } from 'react-native';
import { container, title } from '../config/GlobalStyles';

export default function NotesScreen() {
  return (
    <View style={container}>
      <Text style={title}>Notes Screen</Text>
    </View>
  );
}